@model HealthCare.DAL.Models.Patient

<h1>Patient Deltails</h1>
<table class="table">
    <thead>
        <tr>
            <th>PatientName</th>
            <th>PatientAge</th>
            <th>PatientGender</th>
            <th>Address</th>
        </tr>
    </thead>
    <tbody>

                <tr>
                    <td>@Model.PatientName</td>
                    <td>@Model.PatientAge</td>
                    <td>@Model.PatientGender</td>
                    <td>@Model.Address</td>
                </tr>
    </tbody>
</table>